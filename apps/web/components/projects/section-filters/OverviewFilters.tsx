'use client';

import { type ReactNode } from 'react';

type OverviewFiltersProps = {
  children: ReactNode;
};

export function OverviewFilters({ children }: OverviewFiltersProps) {
  return <>{children}</>;
}

